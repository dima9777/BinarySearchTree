install: make get-deps
./configure && make && make test
language: cpp

compiler:
  - clang
  - gcc
  
addons:
  apt:
    packages:
      - cmake
  
script: 
  - mkdir build
  - cd build 
  - cmake .. && make
